---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "barndoor_dlp_allow_list_entry Resource - Barndoor"
subcategory: ""
description: |-
  Manages one Data Protection (DLP) allow-list entry: a literal value or regular expression that detection findings are suppressed for, optionally limited to specific detection types.
  The platform API has no update endpoint for allow-list entries, so changing any attribute replaces the entry (delete + create).
---

# barndoor_dlp_allow_list_entry (Resource)

Manages one Data Protection (DLP) allow-list entry: a literal value or regular expression that detection findings are suppressed for, optionally limited to specific detection types.

The platform API has no update endpoint for allow-list entries, so **changing any attribute replaces the entry** (delete + create).

## Example Usage

```terraform
# Suppress email-address findings for a shared support mailbox. The platform
# API has no update endpoint for allow-list entries, so changing any attribute
# replaces the entry.
resource "barndoor_dlp_allow_list_entry" "support_mailbox" {
  pattern      = "support@example.com"
  pattern_type = "PATTERN_TYPE_LITERAL"

  # Omit detection_types to suppress matches for every detection type.
  detection_types = ["DETECTION_TYPE_EMAIL"]

  reason = "Shared support mailbox, not PII"
}

# Regex entries allow whole families of values.
resource "barndoor_dlp_allow_list_entry" "test_cards" {
  pattern      = "^4111[ -]?1111[ -]?1111[ -]?1111$"
  pattern_type = "PATTERN_TYPE_REGEX"
  reason       = "Documented test card number"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `pattern` (String) The value to allow: a literal string or a regular expression, per `pattern_type`. Changing it forces a new entry.
- `pattern_type` (String) How `pattern` matches: `PATTERN_TYPE_LITERAL` or `PATTERN_TYPE_REGEX`. Changing it forces a new entry.

### Optional

- `detection_types` (List of String) Detection types the entry suppresses (built-in `DETECTION_TYPE_*` names or an organization's custom detection type names). Omit to suppress matches for every detection type. Changing it forces a new entry.
- `reason` (String) Why the value is allowed (free-form, for audit context). Changing it forces a new entry.

### Read-Only

- `created_at` (String) When the entry was created (RFC 3339).
- `created_by` (String) Subject that created the entry (empty when the platform could not attribute it).
- `id` (String) Entry UUID assigned by the API; also the `terraform import` key.
- `org_id` (String) Organization the entry belongs to, resolved from the provider credential's token claims.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import by the allow-list entry's UUID (the resource's id). The API has no
# get-by-id endpoint, so the provider finds the entry by walking the paginated
# allow list.
terraform import barndoor_dlp_allow_list_entry.support_mailbox 11111111-1111-1111-1111-111111111111
```
